{
  "name": "claude-bot",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "start": "bun main.ts",
    "dev": "bun --watch main.ts",
    "migrate": "bun memory/db.ts",
    "cli": "bun cli.ts",
    "test": "cd tests && npx playwright test --config=playwright.config.ts",
    "test:api": "cd tests && npx playwright test --config=playwright.config.ts --project=api",
    "test:dashboard": "cd tests && npx playwright test --config=playwright.config.ts --project=dashboard",
    "cleanup:dry-run": "DRY_RUN=true bun cleanup/runner.ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "@grammyjs/auto-retry": "^2.0.2",
    "@modelcontextprotocol/sdk": "^1.12.1",
    "grammy": "^1.35.0",
    "postgres": "^3.4.5",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
